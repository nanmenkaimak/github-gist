type: google.api.Service
config_version: 3

http:
  rules:
    - selector: userservice.UserService.GetUserByUsername
      post: "/grpc/v1/getUserByLogin"
      body: "*"
    - selector: userservice.UserService.CreateUser
      post: "/grpc/v1/CreateUser"
      body: "*"
